# Set to the HTTPS origin of your purchased domain before deploying.
# Leave empty for local development; no production domain is assumed.
NEXT_PUBLIC_SITE_URL=

# Set to 1 and rebuild to show maintenance across the site; unset and rebuild to reopen.
SKOUT_MAINTENANCE=0
